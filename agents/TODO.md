# agents/TODO.md

**Superseded by `TASKS.md` at the repository root, 2026-09-19.**

The camera pivot reshaped the work queue enough that keeping a second, agent-only list would guarantee the two disagree by morning.
`TASKS.md` is the single board: dependency-ordered, claimable, and it carries the agent work in Lane A.

What this file said that is still true and is now stated there:

- Deployment is the hard track requirement and it is still open. `TASKS.md` T11
- The verification order's two halves are built and passing. Nothing in the pivot touched them
- Real certificates, DANE and a stapled receipt are what move us Bronze to Gold. T12, T62
- Certificate drift detection is the cheapest demonstrable use of ANS available to us and is still not surfaced. T18
- The agent layer must never block on the hardware. Still true, and the pivot makes it more important, because there is now twice as much hardware

Do not add tasks here.
